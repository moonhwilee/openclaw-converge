#!/usr/bin/env python3
from pathlib import Path
import os
import sys

root = str(Path(__file__).resolve().parents[1])
sys.path.insert(0, root)
os.environ.setdefault("OPENCLAW_CONVERGE_SOURCE_ROOT", root)

from converge.cli import main

raise SystemExit(main())
